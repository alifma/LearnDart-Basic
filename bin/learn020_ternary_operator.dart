// Ternary oeprator, if else dalam satu baris, elvis operator kalau di kotlin
void main() {
  var nilai = 75;
  String ucapan;
  // kondisi ? jika true : jika false;
  ucapan = nilai >= 75 ? 'Anda Lulus' : 'Anda gagal';
  print(ucapan);
}
